# Redis Cluster Configuration Example
# Copy this file to .env and customize the values for your environment

# IP addresses for each physical node in the Docker Swarm cluster
# Each node will run 2 Redis instances (1 master + 1 replica)
NODE_B1_IP=192.168.0.12  # Physical node b1 - runs redis1, redis4
NODE_B2_IP=192.168.0.13  # Physical node b2 - runs redis2, redis5
NODE_B3_IP=192.168.0.14  # Physical node b3 - runs redis3, redis6

# Redis authentication password
# Use a strong password for production environments
# Generate secure password: openssl rand -base64 32 | tr -d "=+/" | cut -c1-25
# Alternative: pwgen -s 32 1 (if pwgen is installed)
# Example: MySecurePassword123, Redis2024!, etc.
REDIS_PASSWORD=your_secure_password_here

# Keycloak OAuth2 Configuration for Redis Insight
# Configure these values according to your Keycloak setup
KEYCLOAK_ISSUER_URL=https://keycloak.yourdomain.com/realms/your-realm
KEYCLOAK_CLIENT_ID=redis-insight-client
KEYCLOAK_CLIENT_SECRET=your-keycloak-client-secret

# OAuth2 Proxy Cookie Secret
# Generate exactly 32 bytes with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# Alternative: openssl rand -hex 16 (produces 32 hex characters = 16 bytes)
# Alternative: openssl rand -base64 24 | tr -d "=" | head -c 32
OAUTH2_COOKIE_SECRET=your-oauth2-cookie-secret-32-chars-exactly
