# Redis Cluster Configuration Example
# Copy this file to .env and customize the values for your environment

# IP address of the Redis cluster master node
# Example: 192.168.1.100, 10.0.0.50, or your server's IP
IP=192.168.0.12

# Redis authentication password
# Use a strong password for production environments
# Generate secure password: openssl rand -base64 32 | tr -d "=+/" | cut -c1-25
# Alternative: pwgen -s 32 1 (if pwgen is installed)
# Example: MySecurePassword123, Redis2024!, etc.
REDIS_PASSWORD=your_secure_password_here

# Redis cluster port range
# Default cluster setup uses 6 consecutive ports starting from PORT_START
# Ports 7001-7006 for data, 17001-17006 for cluster bus communication
PORT_START=7001
PORT_END=7006

# Optional: Redis host for Spring Boot applications
# Uncomment and set if different from IP above
REDIS_HOST=redis-cluster.yourdomain.com
REDIS_PORT=6379
